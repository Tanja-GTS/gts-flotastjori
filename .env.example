# Frontend (Vite)
# Leave these blank to run without login (dev-friendly).

# Entra ID (Azure AD) app registration for the SPA (frontend)
VITE_ENTRA_TENANT_ID=
VITE_ENTRA_CLIENT_ID=

# The scope for calling your backend API from the SPA.
# Example (recommended): api://<BACKEND_API_APP_CLIENT_ID>/access_as_user
VITE_ENTRA_API_SCOPE=

# Optional: backend base URL for non-proxied deployments.
# In local dev, leave empty and Vite will proxy /api to http://localhost:4000
VITE_BACKEND_URL=
